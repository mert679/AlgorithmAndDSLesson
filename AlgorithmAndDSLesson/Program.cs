

using AlgorithmAndDSLesson;
using System;

var task = new LinkedList();
task.AddElement(5);
task.AddElement(10);
task.AddElement(15);
task.AddElement(20);
task.AddElement(25);
task.AddElement(30);
task.AddElement(35);
task.display();
Console.WriteLine("\nLength :  " + task.findLength());